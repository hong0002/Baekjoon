# 입력 받은 이진수를 정수로 변환한 후 17을 곱하고 다시 이진수로 변환하여 출력하는 코드

# 이진수 N 입력 받기 (문자열 형태)
N = input().strip()

# 이진수를 10진수 정수로 변환
num = int(N, 2)

# 17을 곱하기
result = num * 17

# 결과를 이진수로 변환 후 '0b' 접두사 제거하고 출력
print(bin(result)[2:])
